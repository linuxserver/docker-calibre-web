#!/usr/bin/with-contenv bash

# put database to /config
[[ ! -e /config/app.db ]] && \
	cp /defaults/app.db /config/app.db \

# link to /app/calibre-web/app.db
ln -s /config/app.db /app/calibre-web/app.db

# permissions
chown -R abc:abc \
	/config \
        /app/calibre-web
